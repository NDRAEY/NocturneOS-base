#pragma once

#include <common.h>

extern size_t VERSION_MAJOR;   /// Версия ядра
extern size_t VERSION_MINOR;   /// Пре-релиз
extern size_t VERSION_PATCH;   /// Патч

extern char* OS_ARCH;     /// Архитектура
extern char* VERSION_NAME;/// Имя версии (изменяется вместе с минорной части версии)

